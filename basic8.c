/* c program to accept a character from the user and display its ASCII value */
#include<stdio.h>
void main()
{
	char ch;
	printf("enter character=");
	scanf("%c",&ch);
	printf("ASCII value of given character=%d",ch);
}
